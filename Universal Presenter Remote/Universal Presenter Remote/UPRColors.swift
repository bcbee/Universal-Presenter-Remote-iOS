//
//  UPRColors.swift
//  Universal Presenter Remote
//
//  Brand colors shared by the iOS and watchOS apps.
//

import SwiftUI

extension Color {
    /// Brand magenta used for primary buttons and accents.
    static let uprPrimary = Color("BrandPrimary")
    /// Green used for the "connected" status indicator.
    static let uprConnected = Color("Connected")
    /// Near-black background of the Present screen.
    static let uprPresentBackground = Color("PresentBackground")
    /// Card / secondary surface on the Present screen.
    static let uprPresentCard = Color("PresentCard")
    /// Light purple fill behind each token digit.
    static let uprTokenBoxBackground = Color("TokenBoxBackground")
}
